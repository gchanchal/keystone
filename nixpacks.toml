# Nixpacks configuration for Railway deployment
[phases.setup]
nixPkgs = ["nodejs_20", "python312", "python312Packages.pip"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build", "chmod +x start.sh"]

[start]
cmd = "bash start.sh"
